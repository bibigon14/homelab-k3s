apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "wc2026bot.fullname" . }}-env
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-secrets
    kind: ClusterSecretStore
  target:
    name: {{ include "wc2026bot.fullname" . }}-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: wc2026bot-secrets
