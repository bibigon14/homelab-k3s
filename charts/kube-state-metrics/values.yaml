image:
  repository: registry.k8s.io/kube-state-metrics/kube-state-metrics
  tag: v2.13.0

namespace: homelab

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi

service:
  port: 8080
