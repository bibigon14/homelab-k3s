apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sre-tls
  namespace: monitoring
spec:
  secretName: sre-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
  - grafana.sre.dstepanov.dev
